<section class="page-title bg-3">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="block">

                    <h1>{{ trans(template.meta_title) }}</h1>
                    <p>{{ trans(template.meta_description) }}</p>


<!--
                    {% if template.breadcrumbs.count() %}
                        <ol class="breadcrumb">
                            {% for breadcrumb, href in template.breadcrumbs %}
                                {% if loop.last ? 'active' %}
                                    <li class="active">
                                        {{ trans(breadcrumb) }}
                                    </li>
                                {% else %}
                                    <li>
                                        <a href="{{ href }}">
                                            {{ trans(breadcrumb) }}
                                        </a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ol>
                    {% endif %}

                    -->
                </div>
            </div>
        </div>
    </div>
</section>